streamlit
    ```
  
  
    Scroll down to the bottom of the upload page and click the green button that says **Commit changes** (this is just the programmer word for "Save"). Your code files are now officially on the internet!
  

{/* Reason: Procedural sequence linking the code repository to the hosting platform to generate a live web URL. */}
  
    Go to [share.streamlit.io](https://share.streamlit.io) and click **Continue with GitHub**. It will ask for permission to view your repository—click authorize!
  
  
    Click the **Create app** button. In the setup window, click the **Repository** box—your new repository name (`quantum-step-hud`) should pop right up! In the **Main file path** box, make sure it says exactly `app.py`.
  
  
    Click the green **Deploy!** button at the bottom.
